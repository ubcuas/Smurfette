version: "3"

services:
  smurfette:
    container_name: smurfette
    hostname: smurfette
    build:
      context: ../
    command: ["./smurfette"]
    tty: true
    networks:
      - gcomnet

networks:
  gcomnet:
    external:
      name: gcom-x_uasnet
